= simple_form_for @organ, :remote => true do |f|
  = input f, :name
  = f.hidden_field :parent_id
  = f.button :submit, "保存"

coffee:
  $("form[data-remote~=true]").bind("ajax:complete",
    (status, xhr) ->
      $('#content').html(xhr.responseText)
      # refresh sidebar
      sidebar = $.ajax({type: "GET", url: "/organs/render_event_response?source=organ_tree&type=refresh", async: false}).responseText;
      $("#sidebar .tree").html(sidebar)
      )
  
  